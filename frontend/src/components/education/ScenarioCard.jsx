/**
 * src/components/education/ScenarioCard.jsx — Single scenario display card.
 * Placeholder for Phase 4.
 * TODO (Phase 4): Card with difficulty badge, title, category, start button.
 */
function ScenarioCard({ scenario, onStart }) {
    return <div className="card">ScenarioCard — {scenario?.title} — TODO Phase 4</div>
}
export default ScenarioCard
